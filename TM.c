#include<stdio.h>
#include<conio.h>
#define f(x) 1/(1+pow(x,2))
void main ()
{
float a,b,h,s=0,newx,integral;
int n,i;
printf("ENTER THE LOWER AND UPPER LIMITS OF INTEGRATION\n");
scanf("%f%f",&a,&b);
printf("ENTER THE NUMBER OF STRIPS=");
scanf("%f",&n);
h=(b-a)/n;
i=n-1;
while(1!=0)
           {
               new x=a+(i*h);
              s=s+f(newx);
              i=-j;
            }
              interval=(h/2)*f(a)+(2*s)+f(b));
              printf("\t THE INTERVAL IS=%f",interval);
              while(fabs(f2)>e&&n<=10);
              return0;
}
